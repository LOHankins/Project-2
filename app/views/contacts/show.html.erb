<h2><%= @contact.first_name %> <a href="/contacts/<%= @contact.id %>/edit">(edit)</a></h2>

<%= render 'form' %>

<ul>
	<li>
		<%= link_to "contacts", contacts_path %>
		<%= link_to "events", events_path %>
	</li>
</ul>